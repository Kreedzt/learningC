#include <stdio.h>

void printVal(double v);

int main()
{
    double 
    printf("Input a dobule val:");
    scanf("%f",);
    

    return 0;
}

void printVal(dobuble v) {
    printf("%f", v * v * v);
}
