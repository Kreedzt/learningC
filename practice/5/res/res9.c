#include <stdio.h>

int main()
{
    int x = 0;
    int a = 0, b = 0, c = 0;

    // a
    x += 10;

    // b
    x += 1;

    // c
    c = 2 * (a + b);

    // d
    c = a * 2 + b * 2;

    return 0;
}
