#include <stdio.h>

int main()
{
    int x = 0;
    float n, k, m;

    n = k = m = 0;

    // a
    x -= 1;
    
    // b
    m = n / k;

    // c
    p = q / b - a;

    // d
    x = (a + b) / (c * d);

    return 0;
}
